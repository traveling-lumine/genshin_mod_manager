enum RouteNames {
  category,
  categoryHero,
  home,
  license,
  loading,
  firstpage,
  nahidaStore,
  setting;
}

enum RouteParams {
  category,
  categoryHeroTag,
}

const protocol = 'gmm-interop-uri';
